# 应用发布与真实页面验收 · 2026-09-14

结论：公开整理后的应用制品已启用，8 项真实页面回归和新增 10 项真实页面流程检查通过。测试连接实际应用、openGauss、独立 IPFS 与 Fabric；业务记录和附件均为标记清楚的模拟材料，未使用接口替身。

## 发布与回退

应用源码基线为 [3302a53](https://github.com/mikewang68/Project_B/commit/3302a53d250a4b2d5df36cc4da155ef13dabb2b1)。发布标识为 `20260914T080403Z`。本轮新增的测试入口和验收文档不改变该应用制品。

候选制品在独立目录构建，前端类型检查与正式构建、后端 Maven clean verify 通过；后端 4 个单元测试通过，1 个需单独真实数据库的集成测试按设计跳过。合约与数据库结构未修改。本轮只切换应用，IPFS、Fabric、数据库和原有连接继续运行。

启用后应用健康，三个依赖均为 UP。发布前后逐条核对 19 条既有记录，事件摘要、证据清单 CID/摘要、交易标识和文件/上链状态一致。应用切换命令耗时 15.235 秒，包含 SSH 和健康等待，不作为停机时长或恢复指标。

应用 JAR SHA-256：`38b005f754556273c3c4ad63ad98a7f0693116c92724ff06ea19e0dd94fd996d`。

应用节点工程目录中的 `runtime/releases/20260914T080403Z/` 保存候选制品、原制品、摘要及本次源码包。原制品为 `previous.jar`，私有发布记录和回退命令保存在维护者本机 `.local/test-results/release-20260914T080403Z/`。需要回退时，在应用节点工程目录执行以下步骤；它会短暂停止应用：

```bash
set -euo pipefail
release_dir=runtime/releases/20260914T080403Z
test -s "$release_dir/previous.jar"
bash deploy/application.sh stop
cp "$release_dir/previous.jar" artifacts/.trust-platform.rollback
mv artifacts/.trust-platform.rollback artifacts/trust-platform.jar
bash deploy/application.sh start
bash deploy/application.sh status
```

本轮正常启用，没有触发回退；保留回退制品不等同于完成协调备份恢复验收。

## 实际检查

浏览器使用无界面 Chrome 152.0.7977.83。原有 `browser-smoke.cjs` 的 8 项检查通过，覆盖登录、台账、核验展示、溯源、录入页面、任务、状态及 390 像素宽度。

新增 `browser-workflow-real.cjs` 的完整成功运行时间为北京时间 2026-09-14 16:14:05 至 16:15:07。每项操作通过真实页面执行，辅以已登录会话的只读状态查询；录入、上传、导入、更正、下载、核验和导出均由页面触发。

| 检查 | 可核实结果 |
|---|---|
| 录入员登录 | 已登录且三个实际依赖健康 |
| PDF/PNG 上传 | 两份模拟附件可靠接收，返回摘要与本机原件一致 |
| 手工录入到货 | 100 吨事件关联两份附件，文件保存和真实链登记完成 |
| JSON 导入 | 1 条验收事件接收、存证完成，证据关联一致 |
| CSV 导入 | 60 吨与 40 吨两条发运事件接收、存证完成 |
| 追加更正 | 补充到货地点，新版本为 v2；原文及原交易未改变，两版本在线核验通过，历史按钮可回看 v1 |
| 附件下载 | 从页面下载的两份文件与模拟原件逐字节一致 |
| 正反向查询 | 来源批次、两个发运批次及两个交接单共 5 次查询，均返回同组 5 条事件，无缺失关联或截断 |
| 核验与导出 | 两次发运在线核验通过；页面下载两份 ZIP，离线工具逐文件核验，并与导出前另一次在线查询所得清单摘要比对通过 |
| 最终状态 | 成功运行新增 5 条事件、5 个完成任务，依赖健康、页面无脚本错误 |

一次完整成功运行新增 5 条事件，其中包括 1 条更正。脚本调试阶段另保留了 5 条带 `UI-` 标记的模拟事件；它们不计入完整流程通过次数，也不作真实业务库存。结束时应用共 29 条事件。详细记录、截图、输入样例和两份原始 ZIP 留在 `.local/test-results`，未修改证据包内容进行脱敏。

## 复现与边界

按模块 README 建立应用访问通道和私有账号文件，在前端目录运行 `pnpm test:e2e:write`。该脚本每次使用新的模拟批次，失败时保留已经接收的事件；只用于独立开发环境。`TRUST_PYTHON` 指定离线核验工具使用的 Python，详细结果在 `.local/test-results/UI-<本次标识>/workflow-real.json`。失败信息会遮盖配置中的账号密码。

该结论补齐真实页面写入操作。协调备份恢复、确定性的链确认超时、强制退出后租约竞争及真实仓库文件缺失/替换仍待专项验收；六系统真实业务适配仍待接入。
