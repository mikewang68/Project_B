# 公开导入检查 · 2026-09-14

本次将可信存证与溯源模块作为 Project_B/TRUST 普通目录导入，公开版本使用配置模板；实际配置、账号和详细证据留在 Git 排除目录，原独立工程及其历史保留在维护者本机。

## 已通过

| 检查 | 结果 |
|---|---|
| 配置与打包 | 8 项自动检查通过：角色/端口映射、示例配置拒绝、必填项、无效输入、独立 IPFS、缺失文件不回退、PowerShell BOM 兼容、排除父仓库及私有/构建目录 |
| 前端 | pnpm 锁文件安装、TypeScript 检查与 Vite 正式构建通过 |
| 后端 | Java 17 / Maven clean verify 通过；4 个单元测试通过，1 个需真实数据库的集成测试按设计跳过 |
| 合约 | Go 测试与合约编译通过 |
| 部署工具 | PowerShell 解析、Bash 语法、Python 编译及配置工具检查通过 |
| 环境映射 | 四节点 SSH 别名对应的地址/账号、项目目录和服务监听检查通过；应用来源地址、健康检查与三个现有转发监听一致 |
| 发布内容 | 新增文件未发现实际部署标识、开发账号密码、常见私钥或访问令牌；Markdown 本地链接检查通过 |
| 证据保留 | 两份原始发运证据包与本机迁移前字节一致；详细资料未并入公开文件 |

构建在与现有服务分开的验证目录执行，未启动新应用或重启当前服务。本次没有执行真实业务写入、停机、备份恢复和异常演练；2026-09-13 的业务验收仍作为独立历史结论。

本轮仅改变运行状态中的节点职责显示文字，业务 API 结构、事件摘要格式、存证合约和数据库迁移语义保持一致。配置化的初始化、备份和故障测试入口只检查配置与语法，没有在新环境重新执行整个安装流程。

公开摘要没有附带内部地址、运行日志和原始账本证据。后续复测默认输出到 `.local/test-results`，由维护者审核后更新公开摘要。
