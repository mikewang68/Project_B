import type {
  ConfigAuditSlice,
  ExceptionSlice,
  InterlockSlice,
  OfflineSlice,
  PlanSlice,
  RecommendationSlice,
  ReportSlice,
  ResourceSlice,
  ScenarioSlice,
  SessionSlice,
  SystemConfigSlice,
  VehicleSlice,
  WorkOrderSlice,
} from './types';

export function createSessionSlice(value: SessionSlice): SessionSlice {
  return value;
}

export function createScenarioSlice(value: ScenarioSlice): ScenarioSlice {
  return value;
}

export function createPlanSlice(value: PlanSlice): PlanSlice {
  return value;
}

export function createRecommendationSlice(value: RecommendationSlice): RecommendationSlice {
  return value;
}

export function createWorkOrderSlice(value: WorkOrderSlice): WorkOrderSlice {
  return value;
}

export function createResourceSlice(value: ResourceSlice): ResourceSlice {
  return value;
}

export function createVehicleSlice(value: VehicleSlice): VehicleSlice {
  return value;
}

export function createExceptionSlice(value: ExceptionSlice): ExceptionSlice {
  return value;
}

export function createInterlockSlice(value: InterlockSlice): InterlockSlice {
  return value;
}

export function createOfflineSlice(value: OfflineSlice): OfflineSlice {
  return value;
}

export function createReportSlice(value: ReportSlice): ReportSlice {
  return value;
}

export function createSystemConfigSlice(value: SystemConfigSlice): SystemConfigSlice {
  return value;
}

export function createConfigAuditSlice(value: ConfigAuditSlice): ConfigAuditSlice {
  return value;
}
