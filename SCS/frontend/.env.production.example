# Production defaults use the same browser origin and are proxied by Nginx/Easegress.
VITE_API_BASE=/api/v1
VITE_WS_PATH=/ws/live

# Use an absolute URL only when WebSocket is published on a different origin.
# VITE_WS_URL=wss://demo.example.com/ws/live
